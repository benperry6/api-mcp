# 系统级要求

规划执行需求开始时，都要使用curl --noproxy "*" 请求地址 https://wiki.cjdropshipping.cn/plugins/viewstorage/viewpagestorage.action?pageId=88507465 查看最新执行要求，规划任务给subagent也需告知subagent需要阅读；
1、文档需要完整阅读一遍，分析出完整任务后，在当前文档最后阅读位置插入阅读标记“# 26年1月1日 xx:xx:xx(取系统实时日期和时间) ，第x次提交(补充需求时，加上第x次补充)\n👨‍💻**提交人：${git username 运行git命令获取}**\n ## 规划任务: \n### xxxx(任务1)\n### xxxx(任务2)“；
2、任务完成后在第1条规则插入末尾插入执行结果的时间及状态，例子“## 任务处理结果: \n > ### 📄xxxx 处理结果（使用引用任务信息，给出完成内容，如果是排查问题，直接写出排查依据和结果） \n🛑**26年1月1日 xx:xx:xx 第x次提交已完成**”。 
3、完成每个任务和本文档写入后，使用“ `wc -l`当前文件行号或`tail` 当前文档尾部”的方式检查最新补充，如果存在更新或者补充要求，继续执行第1、2条规则，否则执行下一条规则；
4、每次文档更新需要遵循TDD开发模式及本文档`系统级要求`要求重新规划完成任务；规划最后一个任务必须是使用askQuestions工具Multi-select模式要求我补充需求（首选项：阅读 ${当前文档路径} 完成补充任务，如果用户选择此项表示用户在当前文档补充了新需求，需要按照要求重新读取文档内容，如果没有在尾部读取到新内容可能用户未保存，需重新调用askQuestions工具询问，Multi-select模式列出建议的后续任务），检查任务完整性不得跳过。

# 任务描述：目前已经知道，当前的 mcp 在走 chatGPT的 http 请求的方式的时候，请求真正接口的时候，对应的请求头会丢失原始的 ip 地址（采用mcp 部署的云端服务的地址给传递过去了），现在需要给相关的接口请求把原始的用户在 http 中的请求头携带对应的地址给传递过去，后端需要的相关配置：    
    public static final String CLIENT_REQUEST_IP = "client-request-ip";
    public static final String CLIENT_REQUEST_URL = "client-request-url";
    public static final String CLIENT_REQUEST_HOST = "client-request-host"; 
相关的其他点的文档：
key value   示例说明   备注
x-original-url      ingress转发的请求url	
client-request-ip	66.249.66.1	外部请求ip地址	内部调用透传外部header key:x-real-ip 服务间调用需要加上客户原始请求地址
client-request-user-agent	Googlebot	外部请求UA	仅限于node内部调用透传外部header key:userAgent
client-request-host	www.cjdropshipping.com	外部请求主机名	仅限于node内部调用透传外部header key:host
x_forward_for:  172.20.129.142,172.20.129.142	客户原始调用地址填入第一个地址	    如果服务之间首次发起是客户端，那么应该设置这个地址；如果x_forward_for为空，可以取x-real-ip值放入第一个


🛑**26年6月25日 11:22:47 第1次补充已完成**

# 26年07月19日 00:30:55 ，第1次提交
👨‍💻**提交人：东咸**
 ## 规划任务:
### 任务1：现有调用链路分析 —— 定位「原始 IP 丢失」根因
梳理 ChatGPT → MCP HTTP transport → 后端真实接口 的完整同步调用链路，明确请求头在何处被「新建请求」丢弃（http-client 仅带 Content-Type + CJ-Access-Token）。产出根因结论与需透传的 5 个出口清单。

### 任务2：TDD 先行 —— 客户端原始请求上下文模块测试
先写失败用例：`extractClientRequestContext`（从入站 header 提取 client-request-ip/url/host/user-agent 与 x-forwarded-for，含 x-real-ip 优先、xff 首地址兜底、数组头取首值）、`buildClientRequestHeaders`（无上下文返回空、有上下文按后端约定 key 构建）。

### 任务3：实现 `src/utils/client-request-context.ts`
基于 `AsyncLocalStorage` 实现请求作用域上下文（对齐既有 `directTokenStorage` 模式），导出后端约定常量 `client-request-ip / client-request-url / client-request-host / client-request-user-agent` 及 header 提取/构建函数；stdio 模式无上下文时优雅降级。

### 任务4：HTTP 入口注入上下文（`src/mcp-server/index.ts`）
在 `requestHandler` 命中 /mcp 路由后，从 `req.headers` 提取上下文并用 `clientRequestStorage.run(...)` 包裹 GET(SSE)/POST 处理，与既有 `directTokenStorage` 嵌套共存。

### 任务5：全部对外后端出口透传 header
在 5 处出口合并 `buildClientRequestHeaders()`：`http-client.ts`（OpenAPI 主收口）、`logistics.tool.ts` trackInfo、`order.tool.ts` listOrder、`auth.tool.ts` login.html/webLogin（附加透传，不覆盖既有浏览器模拟 UA）。业务零篡改，纯增量 header。

### 任务6：TDD 验收 + 回归 + Build
扩展 `http-client.test.ts` 验证「上下文内出站请求携带 client-request-* header」「无上下文不携带」；跑全量 vitest 回归 + `npm run build` 编译验证。

### 任务7：文档闭环
更新 `docs/测试标准.md`、生成 `测试结果/测试结果<时间戳>_请求头透传客户端原始IP.md`、维护 `docs/工作记录/260719_东咸.md` 日报，并对 diff 做对抗式代码评审。

### 任务8：askQuestions 多选收尾补充需求（不得跳过）

## 任务处理结果:
 > ### 📄任务1（调用链路分析）处理结果
 > 根因：MCP 以 HTTP/HTTPS transport 供 ChatGPT 调用时，服务端向真实后端「新建」HTTP 请求（`http-client.ts` 及 logistics/order/auth 三处 raw fetch），仅携带 `Content-Type` + `CJ-Access-Token`，客户端原始请求信息（IP/host/url/UA）未透传，后端只能读到 MCP 云端 Pod 出口 IP。共定位 **5 个对外后端出口**。stdio 模式无 HTTP 请求，不涉及。
 >
 > ### 📄任务2/3（TDD + 客户端请求上下文模块）处理结果
 > 先写失败用例 `tests/unit/client-request-context.test.ts`（12 例）→ 实现 `src/utils/client-request-context.ts`（`AsyncLocalStorage` + `extractClientRequestContext` + `buildClientRequestHeaders` + 后端约定常量）→ 绿灯。提取规则：`client-request-ip` 优先 `x-real-ip`，否则 `x-forwarded-for` 首地址；`x-forwarded-for` 为空用 `x-real-ip` 兜底放首位；数组头取首值。
 >
 > ### 📄任务4（HTTP 入口注入）处理结果
 > `src/mcp-server/index.ts` 命中 /mcp 路由后 `extractClientRequestContext(req.headers)`，用 `clientRequestStorage.run` 包裹 GET(SSE)/POST 处理，与既有 `directTokenStorage` 嵌套共存。
 >
 > ### 📄任务5（全部对外出口透传）处理结果
 > 5 处出口合并 `buildClientRequestHeaders()`：`http-client.ts`（OpenAPI 主收口）、`logistics.tool.ts` trackInfo、`order.tool.ts` listOrder、`auth.tool.ts` login.html/webLogin（附加透传，不覆盖既有浏览器模拟 UA）。业务零篡改，纯增量 header。
 >
 > ### 📄任务6（TDD 验收 + 回归 + Build）处理结果
 > `vitest run`：新增 15 用例（含真实本地后端 over-the-wire 集成用例）全部通过；140 用例 137 通过、**零回归**；`npm run build` 成功；本地真实 HTTP 服务带 `x-real-ip` 冒烟（initialize/tools/list）正常。
 > ⚠️ 另有 3 个 `auth-tool.test.ts` (`wait_for_login` resourceUri) 用例为**改动前基线即失败**，根因为近期「去掉 apikey/UI 调整」移除登录 UI `_meta.ui.resourceUri` 而测试未同步，**与本需求无关**，已列入收尾建议。
 >
 > ### 📄任务7（文档闭环 + 对抗式评审）处理结果
 > 已更新 `docs/测试标准.md`、生成 `测试结果/测试结果260719003055_请求头透传客户端原始IP.md`、维护 `docs/工作记录/260719_东咸.md`。
 > 对本次 diff 运行 4 维度对抗式代码评审 workflow（16 条发现→核验后仅 1 条真实）：**确认并修复 1 处中危安全缺陷（CWE-532）**——直连 Token 模式下 `client-request-url`（取自 x-original-url）会把长期有效 access token 写入后端日志；已在 `client-request-context.ts` 增加 `sanitizeUrlCredentials()` 将 `@CJ:{token}` 脱敏为 `@CJ:***`（保留 userId 供审计），并补脱敏用例。最终 141 用例 138 通过、新增 16 全部通过、零回归、build 成功。

🛑**26年07月19日 00:45:18 第1次提交已完成**

# 每次运行的结果插入到本段前面，以下为每次阅读时候都要确认没有遗忘的规则要求：

1、每次文档更新需要遵循TDD开发模式及本文档`系统级要求`要求重新规划完成任务；2、规划最后一个任务必须是使用askQuestions工具Multi-select模式要求我补充需求（首选项：阅读 ${当前文档路径} 完成补充任务，如果用户选择此项表示用户在当前文档补充了新需求，需要按照要求重新读取文档内容，如果没有在尾部读取到新内容可能用户未保存，需重新调用askQuestions工具询问，Multi-select模式列出建议的后续任务），检查任务完整性不得跳过。3、允许直接操作本地的软件和git管理的代码，注意操作远程会有修改、删除数据效果并且不可撤销的操作必须先写入完整的操作方案，然后调用askQuestions工具Multi-select模式询问，确认后按照方案操作。4、当出现纠正的时候需要在修改的每个代码文件、方法前面按照规范写入注意事项注释说明，比如java代码需要遵循java doc注释规范，js代码需要遵循js doc注释规范；阅读代码时需要注意这些注释说明，理解修改的原因和目的；5、及时清理掉无用的文件，无用的日志文件，临时文件保存到tmp目录；6、注意检查保密、密钥等信息不要加入git管理，如果存在提示我需要删除；